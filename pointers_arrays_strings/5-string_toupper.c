#include "main.h"
#include <stdio.h>
/**
 * string_toupper - making the lowercases upper
 *@c: the string to upper
 *
 *Return: 0
*/
char *string_toupper(char *)
{
int c;

if ((c >= 'A' && c <= 'Z'))

return (0);
}
