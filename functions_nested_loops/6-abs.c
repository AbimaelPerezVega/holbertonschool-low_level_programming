#include "main.h"
/**
 *_abs - compute a value of an integer
 *@n: integer to compute
 *Return: Always 0
*/
int _abs(int n);
{
if (n < 0)
{
int abs_val;

abs_val = c * -1;
return (abs_val);
}
return (n);
}
